/** Rating and scoring utilities using Wilson Score and other algorithms. */
/** Calculate score for Positive/Negative ratings using Wilson Score Equation (0 to 1). */
export declare const score: (p?: number, n?: number) => number;
/** Calculate score for multi-level ratings (e.g., 1-5 stars) using Wilson Score (0 to 1). */
export declare const rate: (rating?: number[]) => number;
/** Calculate weighted average for multi-level ratings (0 to 5 scale). */
export declare const average: (rating?: number[]) => number;
/**
 * Calculate Bayesian Average rating.
 * Balances between item average and global average.
 */
export declare const bayesianAverage: (ratings?: number[], prior?: number, priorMean?: number) => number;
/** Calculate time-decay weighted rating using exponential decay. */
export declare const weightedRating: (ratings?: Array<{
    rating: number;
    timestamp: number;
}>, halfLife?: number) => number;
/** Calculate viral score for trending content combining engagement with time decay. */
export declare const viralScore: ({ engagement, rating, ageInHours, windowSize, }?: {
    engagement?: number;
    rating?: number;
    ageInHours?: number;
    windowSize?: number;
}) => number;
/** Rating submodule containing all rating and scoring functions. */
export declare const rating: {
    score: (p?: number, n?: number) => number;
    rate: (rating?: number[]) => number;
    average: (rating?: number[]) => number;
    bayesianAverage: (ratings?: number[], prior?: number, priorMean?: number) => number;
    weightedRating: (ratings?: Array<{
        rating: number;
        timestamp: number;
    }>, halfLife?: number) => number;
    viralScore: ({ engagement, rating, ageInHours, windowSize, }?: {
        engagement?: number;
        rating?: number;
        ageInHours?: number;
        windowSize?: number;
    }) => number;
};
