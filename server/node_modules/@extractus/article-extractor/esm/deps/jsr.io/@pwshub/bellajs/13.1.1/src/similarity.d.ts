/** String similarity utilities using cosine similarity. */
/** Tokenizes a string into lowercase alphanumeric words. */
export declare const tokenize: (text: string) => string[];
/** Builds a bag-of-words frequency map from tokens. */
export declare const toBow: (tokens: string[]) => Record<string, number>;
/** Computes cosine similarity between two bag-of-words vectors (0 to 1). */
export declare const cosineSimilarity: (bow1: Record<string, number>, bow2: Record<string, number>) => number;
/** Compares two strings and returns their cosine similarity score (0 to 1). */
export declare const compareTwoStrings: (first: string, second: string) => number;
/** Checks if two strings are similar based on a threshold. */
export declare const isSimilar: (first: string, second: string, threshold?: number) => boolean;
