/** Property definition utility. */
interface DefinePropConfig {
    writable?: boolean;
    configurable?: boolean;
    enumerable?: boolean;
}
/** Defines a non-writable, non-configurable, non-enumerable property by default. */
export declare const defineProp: (ob: object, key: string, val: any, config?: DefinePropConfig) => void;
export {};
