"use strict";
// utils -> transformation.ts
Object.defineProperty(exports, "__esModule", { value: true });
exports.execPostParser = exports.execPreParser = exports.findTransformations = exports.getTransformations = exports.removeTransformations = exports.addTransformations = void 0;
const mod_js_1 = require("../../deps/jsr.io/@pwshub/bellajs/13.1.1/mod.js");
const linkedom_1 = require("linkedom");
const transformations = [];
const add = (tn) => {
    const { patterns } = tn;
    if (!patterns || !(0, mod_js_1.isArray)(patterns) || !patterns.length) {
        return 0;
    }
    transformations.push(tn);
    return 1;
};
/** Register one or more transformations for per-site HTML processing. */
const addTransformations = (tfms) => {
    if ((0, mod_js_1.isArray)(tfms)) {
        return tfms.map((tfm) => add(tfm)).filter((result) => result === 1).length;
    }
    return add(tfms);
};
exports.addTransformations = addTransformations;
/** Remove transformations matching the given patterns. Removes all if no patterns given. */
const removeTransformations = (patterns) => {
    if (!patterns) {
        const removed = transformations.length;
        transformations.length = 0;
        return removed;
    }
    let removing = 0;
    for (let i = transformations.length - 1; i >= 0; i--) {
        const { patterns: ipatterns } = transformations[i];
        const matched = ipatterns.some((ptn) => patterns.some((pattern) => String(pattern) === String(ptn)));
        if (matched) {
            transformations.splice(i, 1);
            removing += 1;
        }
    }
    return removing;
};
exports.removeTransformations = removeTransformations;
const getTransformations = () => {
    return [...transformations];
};
exports.getTransformations = getTransformations;
const findTransformations = (links) => {
    const urls = !(0, mod_js_1.isArray)(links) ? [links] : links;
    const tfms = [];
    for (const transformation of transformations) {
        const { patterns } = transformation;
        const matched = urls.some((url) => patterns.some((pattern) => pattern.test(url)));
        if (matched) {
            tfms.push({
                ...transformation,
            });
        }
    }
    return tfms;
};
exports.findTransformations = findTransformations;
const execPreParser = (html, links) => {
    const doc = new linkedom_1.DOMParser().parseFromString(html, "text/html");
    (0, exports.findTransformations)(links).map((tfm) => tfm.pre).filter((fn) => (0, mod_js_1.isFunction)(fn)).forEach((fn) => fn(doc));
    return Array.from(doc.childNodes).map((it) => it.outerHTML).join("");
};
exports.execPreParser = execPreParser;
const execPostParser = (html, links) => {
    const doc = new linkedom_1.DOMParser().parseFromString(html, "text/html");
    (0, exports.findTransformations)(links).map((tfm) => tfm.post).filter((fn) => (0, mod_js_1.isFunction)(fn)).forEach((fn) => fn(doc));
    return Array.from(doc.childNodes).map((it) => it.outerHTML).join("");
};
exports.execPostParser = execPostParser;
