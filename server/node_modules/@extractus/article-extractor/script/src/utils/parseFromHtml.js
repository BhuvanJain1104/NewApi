"use strict";
// utils -> parseFromHtml.ts
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const mod_js_1 = require("../../deps/jsr.io/@pwshub/bellajs/13.1.1/mod.js");
const html_js_1 = require("./html.js");
const linker_js_1 = require("./linker.js");
const extractMetaData_js_1 = __importDefault(require("./extractMetaData.js"));
const extractWithReadability_js_1 = __importDefault(require("./extractWithReadability.js"));
const transformation_js_1 = require("./transformation.js");
const config_js_1 = require("../config.js");
const fmtIso = (input) => {
    try {
        return (new Date(input)).toISOString();
    }
    catch {
        return input;
    }
};
const summarize = ({ desc, text, threshold, maxlen }) => {
    return desc.length > threshold
        ? desc
        : (0, mod_js_1.truncateByChar)(text, maxlen).replace(/\n/g, " ");
};
// deno-lint-ignore require-await
exports.default = async (inputHtml, inputUrl = "", parserOptions = {}) => {
    const meta = (0, extractMetaData_js_1.default)(inputHtml);
    let title = meta.title;
    const { url, shortlink, amphtml, canonical, description: metaDesc, image: metaImg, author, published, favicon: metaFav, type, } = meta;
    const { wordsPerMinute = 300, descriptionTruncateLen = 210, descriptionLengthThreshold = 180, contentLengthThreshold = 200, allowedTags = config_js_1.defaultAllowedTags, allowedAttributes = config_js_1.defaultAllowedAttributes, allowedIframeDomains = config_js_1.defaultAllowedIframeDomains, } = parserOptions;
    const links = (0, mod_js_1.unique)([url, shortlink, amphtml, canonical, inputUrl]
        .filter(linker_js_1.isValid)
        .map(linker_js_1.purify));
    if (!links.length) {
        return null;
    }
    const bestUrl = (0, linker_js_1.chooseBestUrl)(links, title);
    const normalizedHtml = (0, linker_js_1.normalize)(inputHtml, bestUrl);
    const preTransformedHtml = (0, transformation_js_1.execPreParser)(normalizedHtml, links);
    const readabilityExtracted = (0, extractWithReadability_js_1.default)(preTransformedHtml, bestUrl);
    if (!readabilityExtracted) {
        return null;
    }
    const extractedContent = readabilityExtracted.content;
    const postTransformedHtml = (0, transformation_js_1.execPostParser)(extractedContent, links);
    const content = (0, html_js_1.cleanify)(postTransformedHtml, {
        allowedTags,
        allowedAttributes,
        allowedIframeDomains,
    });
    if (!content) {
        return null;
    }
    if (!title) {
        title = readabilityExtracted.title;
    }
    const textContent = (0, mod_js_1.stripTags)(content);
    if (textContent.length < contentLengthThreshold) {
        return null;
    }
    const description = summarize({
        desc: readabilityExtracted.excerpt || metaDesc,
        text: textContent,
        threshold: descriptionLengthThreshold,
        maxlen: descriptionTruncateLen,
    });
    const image = metaImg ? (0, linker_js_1.absolutify)(bestUrl, metaImg) : "";
    const favicon = metaFav ? (0, linker_js_1.absolutify)(bestUrl, metaFav) : "";
    const imgcount = (0, html_js_1.countImages)(content);
    return {
        url: bestUrl,
        title,
        description,
        links,
        image,
        content,
        author: author || readabilityExtracted.byline || "",
        favicon,
        source: readabilityExtracted.siteName || (0, linker_js_1.getDomain)(bestUrl),
        published: fmtIso(readabilityExtracted.publishedTime || published),
        ttr: (0, mod_js_1.getTTR)(textContent, imgcount, wordsPerMinute),
        type,
    };
};
