"use strict";
// utils -> similarity.ts
Object.defineProperty(exports, "__esModule", { value: true });
exports.findBestMatch = void 0;
const mod_js_1 = require("../../deps/jsr.io/@pwshub/bellajs/13.1.1/mod.js");
const areArgsValid = (mainString, targetStrings) => {
    return (0, mod_js_1.isString)(mainString) && (0, mod_js_1.isArray)(targetStrings) &&
        targetStrings.length > 0 &&
        targetStrings.every((s) => (0, mod_js_1.isString)(s));
};
const findBestMatch = (mainString, targetStrings) => {
    if (!areArgsValid(mainString, targetStrings)) {
        throw new Error("Bad arguments: First argument should be a string, second should be an array of strings");
    }
    const ratings = [];
    let bestMatchIndex = 0;
    for (let i = 0; i < targetStrings.length; i++) {
        const currentTargetString = targetStrings[i];
        const currentRating = (0, mod_js_1.compareTwoStrings)(mainString, currentTargetString);
        ratings.push({ target: currentTargetString, rating: currentRating });
        if (currentRating > ratings[bestMatchIndex].rating) {
            bestMatchIndex = i;
        }
    }
    const bestMatch = ratings[bestMatchIndex];
    return { ratings, bestMatch, bestMatchIndex };
};
exports.findBestMatch = findBestMatch;
