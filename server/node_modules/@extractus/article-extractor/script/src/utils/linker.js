"use strict";
// utils -> linker.ts
Object.defineProperty(exports, "__esModule", { value: true });
exports.getDomain = exports.normalize = exports.purify = exports.absolutify = exports.chooseBestUrl = exports.isValid = void 0;
const linkedom_1 = require("linkedom");
const similarity_js_1 = require("./similarity.js");
const isValid = (url = "") => {
    try {
        const ourl = new URL(url);
        return ourl !== null && ourl.protocol.startsWith("http");
    }
    catch {
        return false;
    }
};
exports.isValid = isValid;
const chooseBestUrl = (candidates = [], title = "") => {
    const ranking = (0, similarity_js_1.findBestMatch)(title, candidates);
    return ranking.bestMatch.target;
};
exports.chooseBestUrl = chooseBestUrl;
const absolutify = (fullUrl = "", relativeUrl = "") => {
    try {
        const result = new URL(relativeUrl, fullUrl);
        return result.toString();
    }
    catch {
        return "";
    }
};
exports.absolutify = absolutify;
const blacklistKeys = [
    "CNDID",
    "__twitter_impression",
    "_hsenc",
    "_openstat",
    "action_object_map",
    "action_ref_map",
    "action_type_map",
    "amp",
    "fb_action_ids",
    "fb_action_types",
    "fb_ref",
    "fb_source",
    "fbclid",
    "ga_campaign",
    "ga_content",
    "ga_medium",
    "ga_place",
    "ga_source",
    "ga_term",
    "gs_l",
    "hmb_campaign",
    "hmb_medium",
    "hmb_source",
    "mbid",
    "mc_cid",
    "mc_eid",
    "mkt_tok",
    "referrer",
    "spJobID",
    "spMailingID",
    "spReportId",
    "spUserID",
    "utm_brand",
    "utm_campaign",
    "utm_cid",
    "utm_content",
    "utm_int",
    "utm_mailing",
    "utm_medium",
    "utm_name",
    "utm_place",
    "utm_pubreferrer",
    "utm_reader",
    "utm_social",
    "utm_source",
    "utm_swu",
    "utm_term",
    "utm_userid",
    "utm_viz_id",
    "wt_mc_o",
    "yclid",
    "WT.mc_id",
    "WT.mc_ev",
    "WT.srch",
    "pk_source",
    "pk_medium",
    "pk_campaign",
];
const purify = (url) => {
    try {
        const pureUrl = new URL(url);
        blacklistKeys.forEach((key) => {
            pureUrl.searchParams.delete(key);
        });
        return pureUrl.toString().replace(pureUrl.hash, "");
    }
    catch {
        return null;
    }
};
exports.purify = purify;
const normalize = (html, url) => {
    const doc = new linkedom_1.DOMParser().parseFromString(html, "text/html");
    Array.from(doc.getElementsByTagName("a")).forEach((element) => {
        const href = element.getAttribute("href");
        if (href) {
            element.setAttribute("href", (0, exports.absolutify)(url, href));
            element.setAttribute("target", "_blank");
        }
    });
    Array.from(doc.getElementsByTagName("img")).forEach((element) => {
        const src = element.getAttribute("data-src") ?? element.getAttribute("src");
        if (src) {
            element.setAttribute("src", (0, exports.absolutify)(url, src));
        }
    });
    Array.from(doc.getElementsByTagName("source")).forEach((element) => {
        const src = element.getAttribute("src");
        if (src) {
            element.setAttribute("src", (0, exports.absolutify)(url, src));
        }
    });
    return Array.from(doc.childNodes).map((element) => element.outerHTML).join("");
};
exports.normalize = normalize;
const getDomain = (url) => {
    const host = (new URL(url)).host;
    return host.replace("www.", "");
};
exports.getDomain = getDomain;
