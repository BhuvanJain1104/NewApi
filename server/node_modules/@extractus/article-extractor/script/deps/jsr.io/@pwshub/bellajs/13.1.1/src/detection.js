"use strict";
/** Type detection utilities. */
Object.defineProperty(exports, "__esModule", { value: true });
exports.isAbsoluteUrl = exports.isValidUrl = exports.hasProperty = exports.isEmpty = exports.isEmail = exports.isDate = exports.isObject = exports.isFunction = exports.isNil = exports.isUndefined = exports.isNull = exports.isBoolean = exports.isString = exports.isArray = exports.isInteger = exports.isNumber = void 0;
const ob2Str = (val) => {
    return {}.toString.call(val);
};
/** Check if value is a number. */
const isNumber = (val) => {
    return Number(val) === val;
};
exports.isNumber = isNumber;
/** Check if value is an integer. */
const isInteger = (val) => {
    return Number.isInteger(val);
};
exports.isInteger = isInteger;
/** Check if value is an array. */
const isArray = (val) => {
    return Array.isArray(val);
};
exports.isArray = isArray;
/** Check if value is a string. */
const isString = (val) => {
    return String(val) === val;
};
exports.isString = isString;
/** Check if value is a boolean. */
const isBoolean = (val) => {
    return Boolean(val) === val;
};
exports.isBoolean = isBoolean;
/** Check if value is null. */
const isNull = (val) => {
    return val === null;
};
exports.isNull = isNull;
/** Check if value is undefined. */
const isUndefined = (val) => {
    return val === undefined;
};
exports.isUndefined = isUndefined;
/** Check if value is null or undefined. */
const isNil = (val) => {
    return (0, exports.isUndefined)(val) || (0, exports.isNull)(val);
};
exports.isNil = isNil;
/** Check if value is a function. */
const isFunction = (val) => {
    return typeof val === "function";
};
exports.isFunction = isFunction;
/** Check if value is a plain object (not an array). */
const isObject = (val) => {
    return ob2Str(val) === "[object Object]" && !(0, exports.isArray)(val);
};
exports.isObject = isObject;
/** Check if value is a valid date. */
const isDate = (val) => {
    return val instanceof Date && !isNaN(val.valueOf());
};
exports.isDate = isDate;
/** Check if value is a valid email address. */
const isEmail = (val) => {
    const re = /^([\w-]+(?:\.[\w-]+)*)@((?:[\w-]+\.)*\w[\w-]{0,66})\.([a-z]{2,6}(?:\.[a-z]{2})?)$/i;
    return (0, exports.isString)(val) && re.test(val);
};
exports.isEmail = isEmail;
/** Check if value is empty (null, undefined, empty string, empty array, or empty object). */
const isEmpty = (val) => {
    return !val || (0, exports.isNil)(val) ||
        ((0, exports.isString)(val) && val === "") ||
        ((0, exports.isArray)(val) && val.length === 0) ||
        ((0, exports.isObject)(val) && Object.keys(val).length === 0);
};
exports.isEmpty = isEmpty;
/** Check if an object has an own property. */
const hasProperty = (obj, prop) => {
    return Object.prototype.hasOwnProperty.call(obj, prop);
};
exports.hasProperty = hasProperty;
/** Check if a string is a valid URL with HTTP or HTTPS protocol. */
const isValidUrl = (url = "") => {
    try {
        const parsed = new URL(url);
        return parsed !== null && !!parsed.protocol &&
            (parsed.protocol === "http:" || parsed.protocol === "https:");
    }
    catch {
        return false;
    }
};
exports.isValidUrl = isValidUrl;
/** Check if a URL string is absolute (starts with http://, https://, or //). */
const isAbsoluteUrl = (url = "") => {
    const u = String(url);
    return u.startsWith("https://") || u.startsWith("http://") ||
        u.startsWith("//");
};
exports.isAbsoluteUrl = isAbsoluteUrl;
