"use strict";
/** String manipulation utilities. */
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTTR = exports.getWordMap = exports.findWordsInWithRegExp = exports.findWordsIn = exports.getWordCount = exports.getArrayOfWords = exports.getSentences = exports.slugify = exports.stripAccent = exports.ucwords = exports.ucfirst = exports.unescapeHTML = exports.escapeHTML = exports.stripTags = exports.truncate = exports.truncateByByte = exports.truncateByCodePoint = exports.truncateByGrapheme = exports.truncateByChar = exports.truncateByWord = void 0;
const detection_js_1 = require("./detection.js");
const toString = (input) => {
    return !(0, detection_js_1.isString)(input) ? String(input) : input;
};
/**
 * Truncates text to a specified word count using Intl.Segmenter.
 * Works with any language.
 */
const truncateByWord = (text, wordLimit = 0) => {
    if (!text)
        return "";
    if (wordLimit <= 0) {
        return text;
    }
    const segmenter = new Intl.Segmenter(undefined, { granularity: "word" });
    const segments = segmenter.segment(text);
    let wordCount = 0;
    let truncatedText = "";
    for (const { segment, isWordLike } of segments) {
        if (wordCount >= wordLimit) {
            return truncatedText.trim() + "...";
        }
        if (isWordLike) {
            wordCount++;
        }
        truncatedText += segment;
    }
    return truncatedText.trim();
};
exports.truncateByWord = truncateByWord;
/**
 * Truncates text to a specified character count using grapheme clusters.
 * Works with emoji and multi-byte characters.
 */
const truncateByChar = (text, charLimit = 0) => {
    if (!text)
        return "";
    if (charLimit <= 0)
        return text;
    const segmenter = new Intl.Segmenter(undefined, { granularity: "grapheme" });
    const segments = segmenter.segment(text);
    let count = 0;
    let result = "";
    for (const { segment } of segments) {
        if (count >= charLimit) {
            return result + "...";
        }
        result += segment;
        count++;
    }
    return result;
};
exports.truncateByChar = truncateByChar;
/** Alias for truncateByChar. */
exports.truncateByGrapheme = exports.truncateByChar;
/**
 * Truncates text to a specified number of Unicode code points.
 * Safe for PostgreSQL VARCHAR columns.
 */
const truncateByCodePoint = (text, max = 250) => {
    if (!text)
        return "";
    const chars = [...text];
    if (chars.length <= max)
        return text;
    return chars.slice(0, max).join("");
};
exports.truncateByCodePoint = truncateByCodePoint;
/**
 * Truncates text to a specified number of UTF-8 bytes.
 * Safe for byte-constrained storage like VARBINARY columns.
 */
const truncateByByte = (text, maxBytes = 255) => {
    if (!text)
        return "";
    const encoder = new TextEncoder();
    const bytes = encoder.encode(text);
    if (bytes.length <= maxBytes)
        return text;
    const decoder = new TextDecoder("utf-8", { fatal: false });
    let result = decoder.decode(bytes.slice(0, maxBytes));
    if (result.endsWith("\uFFFD")) {
        result = result.slice(0, -1);
    }
    return result;
};
exports.truncateByByte = truncateByByte;
/** @deprecated Use truncateByWord instead. */
exports.truncate = exports.truncateByWord;
/** Removes all HTML tags from a string. */
const stripTags = (s) => {
    return toString(s).replace(/(<([^>]+)>)/ig, "").trim();
};
exports.stripTags = stripTags;
/** Escapes HTML special characters to entity equivalents. */
const escapeHTML = (s) => {
    return toString(s)
        .replace(/&/g, "&amp;")
        .replace(/</g, "&lt;")
        .replace(/>/g, "&gt;")
        .replace(/"/g, "&quot;");
};
exports.escapeHTML = escapeHTML;
/** Unescapes HTML entities back to original characters. */
const unescapeHTML = (s) => {
    return toString(s)
        .replace(/&quot;/g, '"')
        .replace(/&lt;/g, "<")
        .replace(/&gt;/g, ">")
        .replace(/&amp;/g, "&");
};
exports.unescapeHTML = unescapeHTML;
/** Uppercases the first character and lowercases the rest. */
const ucfirst = (s) => {
    const x = toString(s).toLowerCase();
    return x.length > 1
        ? x.charAt(0).toUpperCase() + x.slice(1)
        : x.toUpperCase();
};
exports.ucfirst = ucfirst;
/** Uppercases the first character of each word. */
const ucwords = (s) => {
    return toString(s).split(" ").map((w) => {
        return (0, exports.ucfirst)(w);
    }).join(" ");
};
exports.ucwords = ucwords;
/** Removes accents and diacritical marks from characters. */
const stripAccent = (s) => {
    const accentMap = new Map();
    const lmap = {
        a: "á|à|ả|ã|ạ|ă|ắ|ặ|ằ|ẳ|ẵ|â|ấ|ầ|ẩ|ẫ|ậ|ä|æ",
        c: "ç",
        d: "đ|ð",
        e: "é|è|ẻ|ẽ|ẹ|ê|ế|ề|ể|ễ|ệ|ë",
        i: "í|ì|ỉ|ĩ|ị|ï|î",
        n: "ñ",
        o: "ó|ò|ỏ|õ|ọ|ô|ố|ồ|ổ|ỗ|ộ|ơ|ớ|ờ|ở|ỡ|ợ|ö|ø",
        s: "ß",
        u: "ú|ù|ủ|ũ|ụ|ư|ứ|ừ|ử|ữ|ự|û",
        y: "ý|ỳ|ỷ|ỹ|ỵ|ÿ",
    };
    for (const key in lmap) {
        lmap[key].split("|").forEach((v) => {
            accentMap.set(v, key);
            accentMap.set(v.toUpperCase(), key.toUpperCase());
        });
    }
    // deno-lint-ignore no-control-regex
    return toString(s).replace(/[^\u0000-\u007E]/g, (a) => {
        return accentMap.get(a) || a;
    });
};
exports.stripAccent = stripAccent;
/** Converts a string into a URL-friendly slug. */
const slugify = (s, delimiter = "-") => {
    if (!s)
        return "";
    return (0, exports.stripAccent)(s)
        .normalize("NFKD")
        .replace(/[\u0300-\u036f]/g, "")
        .toLowerCase()
        .replace(/[^a-z0-9]+/g, " ")
        .trim()
        .replace(/\s+/g, delimiter);
};
exports.slugify = slugify;
/** Splits text into sentences using Intl.Segmenter. */
const getSentences = (text, lang) => {
    if (!text)
        return [];
    const segmenter = new Intl.Segmenter(lang, { granularity: "sentence" });
    const segments = segmenter.segment(text.trim());
    return Array.from(segments).map((item) => item.segment);
};
exports.getSentences = getSentences;
/** Extracts words from text using Intl.Segmenter. */
const getArrayOfWords = (text, lang) => {
    if (!text)
        return [];
    const segmenter = new Intl.Segmenter(lang, { granularity: "word" });
    const segments = segmenter.segment(text.trim());
    const words = [];
    for (const { segment, isWordLike } of segments) {
        if (isWordLike) {
            words.push(segment);
        }
    }
    return words;
};
exports.getArrayOfWords = getArrayOfWords;
/** Counts words in text using Intl.Segmenter. */
const getWordCount = (text) => {
    if (!text)
        return 0;
    const segmenter = new Intl.Segmenter(undefined, { granularity: "word" });
    const segments = segmenter.segment(text);
    let wordCount = 0;
    for (const { isWordLike } of segments) {
        if (isWordLike) {
            wordCount++;
        }
    }
    return wordCount;
};
exports.getWordCount = getWordCount;
/** Finds specific words within a text using Intl.Segmenter. */
const findWordsIn = (text, words = []) => {
    if (!text || !words.length)
        return [];
    const textWords = (0, exports.getArrayOfWords)(text).map((w) => w.toLowerCase());
    return words.filter((word) => {
        return textWords.includes(word.toLowerCase());
    });
};
exports.findWordsIn = findWordsIn;
/** Finds specific words within a text using RegExp. */
const findWordsInWithRegExp = (text, words = []) => {
    if (!text || !words.length)
        return [];
    const lowerText = text.toLowerCase();
    return words.filter((word) => {
        const regex = new RegExp("\\b" + word.toLowerCase() + "\\b", "gi");
        return regex.test(lowerText);
    });
};
exports.findWordsInWithRegExp = findWordsInWithRegExp;
/** Creates a frequency map of word occurrences in text. */
const getWordMap = (text) => {
    if (!text)
        return {};
    const words = (0, exports.getArrayOfWords)(text).filter((w) => w.length > 2);
    return words.reduce((acc, curr) => {
        acc[curr] = (acc[curr] ?? 0) + 1;
        return acc;
    }, {});
};
exports.getWordMap = getWordMap;
/** Calculates Time To Read in minutes based on word count. */
const getTTR = (text, imgcount = 0, wordsPerMinute = 268) => {
    const wordCount = (0, exports.getWordCount)(text);
    const minToRead = wordCount / wordsPerMinute;
    const secToRead = Math.ceil(minToRead * 60);
    const secOnImage = imgcount * 5;
    return Math.ceil((secToRead + secOnImage) / 60);
};
exports.getTTR = getTTR;
